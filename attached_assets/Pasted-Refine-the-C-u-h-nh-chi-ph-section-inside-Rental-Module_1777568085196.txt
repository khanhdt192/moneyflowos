Refine the "Cấu hình chi phí" section inside Rental Module for Money Flow OS.

IMPORTANT:
Do not redesign the whole Rental page.
Only improve the Cost Configuration logic + UI.

====================================================
🎯 OBJECTIVE

Split current rental cost settings into 2 separate configuration groups:

1. Shared Configuration for rooms 201 → 305 (10 rooms)
2. Separate Configuration for Ground Floor Room (Tầng 1)

This will better reflect real rental operations.

====================================================
🎯 SECTION A: CONFIG FOR ROOMS 201 → 305

Create a card titled:

"Cấu hình chung cho phòng 201 - 305"

Fields:

1. Điện
- Keep current logic unchanged
- Price per kWh remains as existing setup

2. Nước
- Keep current logic unchanged
- Use current water price setting

3. Wifi
- Change logic to PER ROOM pricing
- Example:
Input 70000 = each occupied room pays 70,000

4. Vệ sinh / Rác
- Change logic to PER ROOM pricing
- Example:
Input 30000 = each occupied room pays 30,000

5. Phụ phí khác
- Change logic to PER ROOM pricing
- Allow:
Fee name + amount
Example:
Parking = 100000 per room

====================================================
🎯 SECTION B: CONFIG FOR GROUND FLOOR ROOM (TẦNG 1)

Create separate card titled:

"Cấu hình riêng cho tầng 1"

Fields:

1. Điện
- Replace normal kWh pricing with:
Input Electricity Bill Code / Invoice Amount

Allow user to manually enter monthly electricity bill.

2. Nước
- Automatically inherit water pricing from shared config above

Display note:
"Dùng chung giá nước với cấu hình phòng 201-305"

3. Wifi
- Allow optional custom price input

If empty / null:
Means tenant uses private internet
=> no wifi charge

4. Vệ sinh / Rác
- Allow custom separate amount

====================================================
🎯 BILLING LOGIC

When generating invoices:

Rooms 201-305:
- Use shared config

Ground Floor:
- Use custom tầng 1 config

====================================================
🎯 UI / UX REQUIREMENTS

Use clean SaaS settings layout:

Two separate cards side-by-side on desktop
Stack vertically on mobile

Each section should have:
- Title
- Description text
- Inputs grouped clearly
- Save button
- Success toast

Use clear labels in Vietnamese.

====================================================
🎯 IMPORTANT

Do not break existing database structure if possible.
Use migration only if required.

Preserve all current rental features.

====================================================
🎯 OUTPUT NOW

Implement these configuration changes with production-ready UI and working billing logic.